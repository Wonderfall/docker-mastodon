--- a/vite.config.mts
+++ b/vite.config.mts
@@ -42,6 +42,7 @@ export const config: UserConfigFnPromise = async ({ mode, command }) => {
       alias: {
         '~/': `${jsRoot}/`,
         '@/': `${jsRoot}/`,
+        blurhash: path.resolve(__dirname, 'node_modules/blurhash/dist/index.js'),
       },
     },
     css: {
